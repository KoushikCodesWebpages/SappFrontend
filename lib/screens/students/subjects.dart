import 'package:flutter/material.dart';

class StuSubjects extends StatelessWidget {
  const StuSubjects({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Student Subject page'),
        ),
      ),
    );
  }
}