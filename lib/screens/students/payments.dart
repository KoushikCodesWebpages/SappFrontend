import 'package:flutter/material.dart';

class StuPayments extends StatelessWidget {
  const StuPayments({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Student Payment page'),
        ),
      ),
    );
  }
}