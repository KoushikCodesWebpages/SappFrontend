import 'package:flutter/material.dart';

class FacPayments extends StatelessWidget {
  const FacPayments({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Faculty Payments'),
        ),
      ),
    );
  }
}