import 'package:flutter/material.dart';

class FacResults extends StatelessWidget {
  const FacResults({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Faculty Results'),
        ),
      ),
    );
  }
}