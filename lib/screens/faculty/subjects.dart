import 'package:flutter/material.dart';

class FacSubjects extends StatelessWidget {
  const FacSubjects({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Faculty Subjects'),
        ),
      ),
    );
  }
}