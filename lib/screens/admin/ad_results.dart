import 'package:flutter/material.dart';

class AdminResults extends StatelessWidget {
  const AdminResults({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('AdminResults'),
        ),
      ),
    );
  }
}