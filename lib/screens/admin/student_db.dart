import 'package:flutter/material.dart';

class AdminStuDb extends StatelessWidget {
  const AdminStuDb({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Admin Student Database'),
        ),
      ),
    );
  }
}