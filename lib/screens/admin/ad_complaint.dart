import 'package:flutter/material.dart';

class AdminComplaint extends StatelessWidget {
  const AdminComplaint({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Admin Complaint Box'),
        ),
      ),
    );
  }
}