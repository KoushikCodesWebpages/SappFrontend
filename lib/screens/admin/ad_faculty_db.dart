import 'package:flutter/material.dart';

class AdminFacDB extends StatelessWidget {
  const AdminFacDB({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Admin Faculty Database'),
        ),
      ),
    );
  }
}